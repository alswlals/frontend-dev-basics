/**
 * Named export I
 *  - default 키워드(X)
 *  - 호환성 없음
 * 
 * 1. exports(require.js)와 유사
 * 2. destructing이 가능
 */

export const add = function(a, b){
    return a + b;
}
export const substract = function(a, b){
    return a - b;
}